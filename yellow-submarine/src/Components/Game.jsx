

import { useRef } from 'react';

import { Parallax, ParallaxLayer } from '@react-spring/parallax'
import '../App.css'

function Game() {

  const ref = useRef();

  return (
    <div>
      FUNCIONA
    </div>
  );
}

export default Game;