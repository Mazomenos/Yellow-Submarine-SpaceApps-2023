import "../App.css";

import { useRef } from 'react';
import { Parallax, ParallaxLayer } from '@react-spring/parallax'

// import '../App.css'

function Game() {

  const ref = useRef();
  return (
    <div id="game-screen">
    </div>
  );
}

export default Game;